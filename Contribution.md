# راهنمای مشارکت در ترجمه کتاب Introduction to Statistics

از اینکه علاقه‌مندید در این پروژه مشارکت کنید خوشحالیم! هدف این پروژه ترجمه‌ی آزاد و متن‌باز کتاب [Introduction to Statistics](https://open.umn.edu/opentextbooks/textbooks/459) به فارسی است تا این منبع در دسترس فارسی‌زبانان بیشتری قرار بگیرد.

## قبل از شروع: انتخاب فصل

برای جلوگیری از دوباره‌کاری (اینکه دو نفر همزمان روی یک فصل کار کنند)، لطفاً قبل از شروع ترجمه:

1. جدول وضعیت فصل‌ها را در [README اصلی پروژه](https://github.com/pouniq/ITS_PersianTranslation) ببینید و مطمئن شوید فصل مورد نظرتان قبلاً برداشته نشده باشد
2. یک [Issue](https://github.com/pouniq/ITS_PersianTranslation/issues) جدید باز کنید و بنویسید کدام فصل را می‌خواهید ترجمه کنید (یا فقط به نگهدارنده‌ی پروژه پیام بدهید)
3. بعد از تأیید، فصل به نام شما در جدول رزرو می‌شود

## مرحله اول: Fork و Clone

ابتدا این repo را **fork** کنید تا نسخه‌ای از آن در پروفایل خودتان داشته باشید.

اگر می‌خواهید فایل‌ها را به‌صورت local داشته باشید، از این دستور در ترمینال استفاده کنید:

```
git clone https://github.com/<your-username>/ITS_PersianTranslation.git
```

## مرحله دوم: ترجمه

ترجمه‌ای که برای هر فصل آماده می‌کنید را در فایل `README.md` مربوط به همان فصل اضافه کنید.

**نکته درباره تصاویر:** برای اینکه تصاویر در README به‌درستی نمایش داده شوند، باید تصویر را در فولدر `imgs` همان فصل قرار دهید و نام‌گذاری را مطابق کتاب اصلی انجام دهید.

### راهنمای اصطلاحات (Glossary)

برای اینکه ترجمه در کل کتاب یکدست بماند، لطفاً از معادل‌های زیر برای اصطلاحات پرتکرار استفاده کنید (این جدول به‌مرور تکمیل می‌شود):

| English | فارسی |
|---|---|
| Variance | واریانس |
| Standard Deviation | انحراف معیار |
| Mean | میانگین |
| Median | میانه |
| Sample | نمونه |
| Population | جامعه |
| Distribution | توزیع |
| Hypothesis | فرضیه |

اگر به اصطلاحی برخوردید که معادل مشخصی برایش وجود ندارد، آن را در Issue مربوطه مطرح کنید تا با هم به توافق برسیم.

### پیشنهاد برای راحت‌تر شدن ترجمه

می‌توانید از **Obsidian** برای ترجمه استفاده کنید. با ویژگی split view می‌توانید کتاب اصلی را در یک سمت و فایل ترجمه را در سمت دیگر داشته باشید:

![split view step 1](https://github.com/pouniq/ITS_PersianTranslation/raw/main/imgs/1.png)

اول روی سه نقطه کلیک کنید و گزینه‌ی **Split Right** را بزنید.

![split view step 2](https://github.com/pouniq/ITS_PersianTranslation/raw/main/imgs/2.png)

سپس به پوشه‌ی `original book` بروید و فایل PDF را باز کنید.

![split view step 3](https://github.com/pouniq/ITS_PersianTranslation/raw/main/imgs/3.png)

حالا در سمت چپ صفحه می‌توانید فایل سمت راست را ترجمه کنید.








### نحوه‌ی درست نمایش تصاویر در گیتهاب

برای اینکه تصویر داخل فایل `README.md` به‌درستی نمایش داده شود، از مسیر نسبی (Relative Path) نسبت به فایل README استفاده کنید:

```markdown
![figure_name](./imgs/1.png)
```

این روش چون به نام branch یا نام کاربری وابسته نیست، حتی اگر repo را fork کنید یا نام branch عوض شود، همچنان کار می‌کند.

**نکات تکمیلی:**
- اسم فایل تصویر را انگلیسی و بدون فاصله بگذارید (`chapter1-fig2.png` نه `شکل ۲.png`)
- اگر می‌خواهید اندازه‌ی تصویر را کنترل کنید، از تگ HTML استفاده کنید چون Markdown ساده امکان تغییر سایز ندارد:

```html
<img src="./imgs/1.png" width="500">
```


اگر از obsidian استفاده میکنید میتوانید به سادگی اندازه تصویر را تغییر دهید، برای انجام اینکار از | در نام عکس استفاده کنید به عنوان مثال:

```markdown
![Table1|300](<./imgs/table1.png>) 
```
که همانطور که میبیند بعد علامت | از یک عدد برای تغییر اندازه عکس استفاده می کنید. که در اینجا 300 است.

- قبل از commit کردن، حتماً پیش‌نمایش (Preview) فایل را در گیتهاب چک کنید تا مطمئن شوید تصویر درست لود می‌شود.






## مرحله سوم: Push

ترجمه‌ای که انجام دادید را در repo خودتان push کنید:

```bash
git add .
git commit -m "ترجمه فصل X"
git push
```

## مرحله چهارم: Pull Request

بعد از اتمام ترجمه، یک **Pull Request** بزنید. در توضیحات PR بنویسید کدام فصل را ترجمه کرده‌اید.

پس از بررسی و تأیید ترجمه، نام یا لینکدین شما (هر کدام که ترجیح بدهید) در جدول مشارکت‌کنندگان در README اصلی پروژه ثبت می‌شود.

## سوالی دارید؟

اگر در هر مرحله سوالی داشتید، یک Issue باز کنید یا مستقیماً پیام بدهید.
